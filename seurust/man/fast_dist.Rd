% Generated by hand for CRAN; keep in sync with R/native.R
\name{fast_dist}
\alias{fast_dist}
\title{Distances between cells and their listed neighbors}
\usage{fast_dist(x, y, n)}
\arguments{\item{x, y}{Numeric matrices of embeddings with the same number of columns (cells in rows).}\item{n}{A list with one element per row of \code{x}, each a vector of 1-based row indices into \code{y}. Integer and double vectors are both accepted.}}
\value{A list the same length (and with the same names) as \code{n}, holding
the distances in the same order as the neighbor indices. An empty list if
\code{length(n) != nrow(x)}, matching Seurat.}
\description{
Rust/extendr reimplementation matching Seurat's native API for \code{fast_dist}.
}
\author{Nebil Ibrahim \email{nebil.b.ibrahim@gmail.com}}
